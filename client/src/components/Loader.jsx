import React from "react";

const Loader = ({ Loader }) => (
  <div className="card container-loader d-flex align-items-center justify-content-center">
    <i className="loader fas fa-star fa-3x text-dark-blue" />
  </div>
);

export default Loader;
